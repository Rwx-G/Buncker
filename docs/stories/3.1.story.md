# Story 3.1: HTTP Server & OCI Distribution API

## Status

Draft

## Story

**As a** Docker build client,
**I want** to pull images from the buncker registry,
**so that** my builds work without internet access.

## Acceptance Criteria

1. `BunckerServer` starts a `ThreadingHTTPServer` with bounded `ThreadPoolExecutor`
2. `GET /v2/` returns 200
3. `GET /v2/{name}/manifests/{reference}` returns manifest with OCI headers
4. `HEAD /v2/{name}/manifests/{reference}` returns 200 or 404
5. `GET /v2/{name}/blobs/{digest}` streams blob with correct headers, updates metadata
6. `HEAD /v2/{name}/blobs/{digest}` returns 200 with `Content-Length` or 404
7. Unmatched requests -> 404
8. Integration tests: server on `localhost:0`, simulated Docker pull

## Tasks / Subtasks

- [ ] Task 1: Implement `BunckerServer` class (AC: 1)
  - [ ] Create `buncker/server.py` with `BunckerServer` class
  - [ ] Use `http.server.ThreadingHTTPServer`
  - [ ] Add bounded `ThreadPoolExecutor` (configurable `max_workers`, default 16)
  - [ ] Accept `bind`, `port`, `store` parameters
  - [ ] Graceful shutdown support
- [ ] Task 2: Implement OCI request handler (AC: 2-7)
  - [ ] Create `buncker/handler.py` with `BunckerHandler(BaseHTTPRequestHandler)`
  - [ ] Route requests based on path pattern matching
  - [ ] `GET /v2/` -> 200 `{"status": "ok"}` (AC: 2)
  - [ ] `GET /v2/{name}/manifests/{reference}` -> lookup manifest, return with headers (AC: 3)
  - [ ] `HEAD /v2/{name}/manifests/{reference}` -> 200 or 404 (AC: 4)
  - [ ] `GET /v2/{name}/blobs/{digest}` -> stream blob, update metadata (AC: 5)
  - [ ] `HEAD /v2/{name}/blobs/{digest}` -> 200 with `Content-Length` or 404 (AC: 6)
  - [ ] Default: 404 for unmatched routes (AC: 7)
- [ ] Task 3: Implement OCI response headers (AC: 3, 5)
  - [ ] `Docker-Content-Digest`: digest of content
  - [ ] `Content-Type`: appropriate OCI media type
  - [ ] `Content-Length`: size in bytes
  - [ ] `Docker-Distribution-API-Version: registry/2.0` on `/v2/`
- [ ] Task 4: Implement blob streaming (AC: 5)
  - [ ] Stream large blobs in chunks (not load entire blob in memory)
  - [ ] Call `store.update_metadata(digest, "pull")` on successful GET
- [ ] Task 5: Input validation
  - [ ] Validate digest format: `^sha256:[a-f0-9]{64}$`
  - [ ] Validate tag format: `^[a-zA-Z0-9._-]{1,128}$`
  - [ ] Path traversal prevention on `{name}` parameter
- [ ] Task 6: Write integration tests (AC: 8)
  - [ ] Start server on `localhost:0` (random port)
  - [ ] Test `GET /v2/` returns 200
  - [ ] Test manifest fetch and head (existing and missing)
  - [ ] Test blob fetch and head (existing and missing)
  - [ ] Test correct OCI headers on responses
  - [ ] Test blob streaming works for large blobs
  - [ ] Test unmatched route returns 404
  - [ ] Simulate a Docker pull sequence: `/v2/` -> manifest -> blobs

## Dev Notes

### OCI Distribution API Specification

| Method | Path | Purpose | Response Headers |
|--------|------|---------|-----------------|
| GET | `/v2/` | Version check | `Docker-Distribution-API-Version: registry/2.0` |
| GET | `/v2/{name}/manifests/{reference}` | Fetch manifest | `Docker-Content-Digest`, `Content-Type`, `Content-Length` |
| HEAD | `/v2/{name}/manifests/{reference}` | Check existence | Same headers, no body |
| GET | `/v2/{name}/blobs/{digest}` | Fetch blob | `Docker-Content-Digest`, `Content-Type`, `Content-Length` |
| HEAD | `/v2/{name}/blobs/{digest}` | Check existence | `Content-Length`, no body |

`{name}` can contain slashes (e.g., `library/nginx`). `{reference}` is a tag or digest.

No authentication in V1 (isolated LAN is the security perimeter).

[Source: `docs/architecture/8-rest-api-spec.md`, `docs/architecture/15-security.md`]

### Server Architecture

- `ThreadingHTTPServer` with bounded `ThreadPoolExecutor` (default 16 workers)
- Same port serves both OCI API and Admin API (differentiated by path prefix)
- Port configurable, default 5000

[Source: `docs/architecture/3-tech-stack.md`, `docs/architecture/5-components.md#buncker`]

### Input Validation

- Digest format: `^sha256:[a-f0-9]{64}$`
- Tag format: `^[a-zA-Z0-9._-]{1,128}$`
- Path traversal prevention on Dockerfile paths and name parameters

[Source: `docs/architecture/15-security.md#Input Validation`]

### Critical Rule

**No internet fallback:** No `urllib.request.urlopen` in `buncker/` package. Missing blob = 404 error. This is by design.

[Source: `docs/architecture/13-coding-standards.md`]

### Dependencies on Previous Stories

- **Story 2.1:** Store with `has_blob()`, `get_blob()`, `update_metadata()`
- **Story 2.3:** Manifest cache with `get_manifest()`
- **Story 1.4:** Exceptions and logging

### Source Tree

```
buncker/
  server.py       # BunckerServer class
  handler.py      # BunckerHandler (BaseHTTPRequestHandler)
tests/
  buncker/
    test_handler.py
    test_server_integration.py
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test locations:** `tests/buncker/test_handler.py` (unit), `tests/buncker/test_server_integration.py` (integration)
- **Framework:** pytest 8.x
- **Integration pattern:** Start server on `localhost:0`, use `urllib.request` to make requests
- **Coverage:** 80% minimum
- Mock store for unit tests, real store with `tmp_path` for integration

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_

## QA Results

_To be filled after QA review_
