# Story 3.1: HTTP Server & OCI Distribution API

## Status

Done

## Story

**As a** Docker build client,
**I want** to pull images from the buncker registry,
**so that** my builds work without internet access.

## Acceptance Criteria

1. `BunckerServer` starts a `ThreadingHTTPServer` with bounded `ThreadPoolExecutor`
2. `GET /v2/` returns 200
3. `GET /v2/{name}/manifests/{reference}` returns manifest with OCI headers
4. `HEAD /v2/{name}/manifests/{reference}` returns 200 or 404
5. `GET /v2/{name}/blobs/{digest}` streams blob with correct headers, updates metadata
6. `HEAD /v2/{name}/blobs/{digest}` returns 200 with `Content-Length` or 404
7. Unmatched requests -> 404
8. Integration tests: server on `localhost:0`, simulated Docker pull

## Tasks / Subtasks

- [x] Task 1: Implement `BunckerServer` class (AC: 1)
  - [x] Create `buncker/server.py` with `BunckerServer` class
  - [x] Use `http.server.ThreadingHTTPServer`
  - [x] Add bounded `ThreadPoolExecutor` (configurable `max_workers`, default 16)
  - [x] Accept `bind`, `port`, `store` parameters
  - [x] Graceful shutdown support
- [x] Task 2: Implement OCI request handler (AC: 2-7)
  - [x] Create `buncker/handler.py` with `BunckerHandler(BaseHTTPRequestHandler)`
  - [x] Route requests based on path pattern matching
  - [x] `GET /v2/` -> 200 `{"status": "ok"}` (AC: 2)
  - [x] `GET /v2/{name}/manifests/{reference}` -> lookup manifest, return with headers (AC: 3)
  - [x] `HEAD /v2/{name}/manifests/{reference}` -> 200 or 404 (AC: 4)
  - [x] `GET /v2/{name}/blobs/{digest}` -> stream blob, update metadata (AC: 5)
  - [x] `HEAD /v2/{name}/blobs/{digest}` -> 200 with `Content-Length` or 404 (AC: 6)
  - [x] Default: 404 for unmatched routes (AC: 7)
- [x] Task 3: Implement OCI response headers (AC: 3, 5)
  - [x] `Docker-Content-Digest`: digest of content
  - [x] `Content-Type`: appropriate OCI media type
  - [x] `Content-Length`: size in bytes
  - [x] `Docker-Distribution-API-Version: registry/2.0` on `/v2/`
- [x] Task 4: Implement blob streaming (AC: 5)
  - [x] Stream large blobs in chunks (not load entire blob in memory)
  - [x] Call `store.update_metadata(digest, "pull")` on successful GET
- [x] Task 5: Input validation
  - [x] Validate digest format: `^sha256:[a-f0-9]{64}$`
  - [x] Validate tag format: `^[a-zA-Z0-9._-]{1,128}$`
  - [x] Path traversal prevention on `{name}` parameter
- [x] Task 6: Write integration tests (AC: 8)
  - [x] Start server on `localhost:0` (random port)
  - [x] Test `GET /v2/` returns 200
  - [x] Test manifest fetch and head (existing and missing)
  - [x] Test blob fetch and head (existing and missing)
  - [x] Test correct OCI headers on responses
  - [x] Test blob streaming works for large blobs
  - [x] Test unmatched route returns 404
  - [x] Simulate a Docker pull sequence: `/v2/` -> manifest -> blobs

## Dev Notes

### OCI Distribution API Specification

| Method | Path | Purpose | Response Headers |
|--------|------|---------|-----------------|
| GET | `/v2/` | Version check | `Docker-Distribution-API-Version: registry/2.0` |
| GET | `/v2/{name}/manifests/{reference}` | Fetch manifest | `Docker-Content-Digest`, `Content-Type`, `Content-Length` |
| HEAD | `/v2/{name}/manifests/{reference}` | Check existence | Same headers, no body |
| GET | `/v2/{name}/blobs/{digest}` | Fetch blob | `Docker-Content-Digest`, `Content-Type`, `Content-Length` |
| HEAD | `/v2/{name}/blobs/{digest}` | Check existence | `Content-Length`, no body |

`{name}` can contain slashes (e.g., `library/nginx`). `{reference}` is a tag or digest.

No authentication in V1 (isolated LAN is the security perimeter).

[Source: `docs/architecture/8-rest-api-spec.md`, `docs/architecture/15-security.md`]

### Server Architecture

- `ThreadingHTTPServer` with bounded `ThreadPoolExecutor` (default 16 workers)
- Same port serves both OCI API and Admin API (differentiated by path prefix)
- Port configurable, default 5000

[Source: `docs/architecture/3-tech-stack.md`, `docs/architecture/5-components.md#buncker`]

### Input Validation

- Digest format: `^sha256:[a-f0-9]{64}$`
- Tag format: `^[a-zA-Z0-9._-]{1,128}$`
- Path traversal prevention on Dockerfile paths and name parameters

[Source: `docs/architecture/15-security.md#Input Validation`]

### Critical Rule

**No internet fallback:** No `urllib.request.urlopen` in `buncker/` package. Missing blob = 404 error. This is by design.

[Source: `docs/architecture/13-coding-standards.md`]

### Dependencies on Previous Stories

- **Story 2.1:** Store with `has_blob()`, `get_blob()`, `update_metadata()`
- **Story 2.3:** Manifest cache with `get_manifest()`
- **Story 1.4:** Exceptions and logging

### Source Tree

```
buncker/
  server.py       # BunckerServer class
  handler.py      # BunckerHandler (BaseHTTPRequestHandler)
tests/
  buncker/
    test_handler.py
    test_server_integration.py
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test locations:** `tests/buncker/test_handler.py` (unit), `tests/buncker/test_server_integration.py` (integration)
- **Framework:** pytest 8.x
- **Integration pattern:** Start server on `localhost:0`, use `urllib.request` to make requests
- **Coverage:** 80% minimum
- Mock store for unit tests, real store with `tmp_path` for integration

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |

## Dev Agent Record

### Agent Model Used

claude-opus-4-6

### Debug Log References

No issues encountered.

### Completion Notes List

- BunckerServer with ThreadingHTTPServer and bounded ThreadPoolExecutor
- BunckerHandler with full OCI Distribution API routing (GET/HEAD for /v2/, manifests, blobs)
- Manifest lookup searches across common platforms (linux/amd64, linux/arm64, linux/arm/v7)
- Blob streaming in 64KB chunks, metadata updated on pull
- Input validation: digest regex, tag regex, path traversal prevention
- 29 tests (9 unit + 20 integration) all passing

### File List

- `buncker/server.py` (new) - BunckerServer class
- `buncker/handler.py` (new) - BunckerHandler with OCI Distribution API
- `tests/buncker/test_handler.py` (new) - Unit tests for handler
- `tests/buncker/test_server_integration.py` (new) - Integration tests

## QA Results

### Review Date: 2026-03-04

### Reviewed By: Quinn (Test Architect)

6 findings: 1 high, 2 medium, 3 low.

### Gate Status

Gate: PASS - docs/qa/gates/3.1-http-server-oci-distribution-api.yml
