# Story 1.1: Project Scaffolding & CI

## Status

Draft

## Story

**As a** developer,
**I want** the project structure, linting, and CI pipeline set up,
**so that** all subsequent development has a solid, tested foundation.

## Acceptance Criteria

1. Repo contains the complete folder structure (`shared/`, `buncker/`, `buncker_fetch/`, `tests/`, `packaging/`)
2. `pyproject.toml` configures ruff (`select E,F,W,I,UP,B,SIM`) and pytest
3. `.gitignore` covers Python (`__pycache__`, `.egg-info`, `dist/`, `*.pyc`, `.pytest_cache`, `.ruff_cache`)
4. GitHub Actions workflow: lint (`ruff check` + `format --check`) + pytest on push/PR
5. A placeholder test passes in CI
6. Makefile with targets: `lint`, `test`, `build` (placeholder)
7. Minimal `README.md` with project name, one-liner description, and dev instructions

## Tasks / Subtasks

- [ ] Task 1: Create complete folder structure (AC: 1)
  - [ ] Create `shared/` with `__init__.py`
  - [ ] Create `buncker/` with `__init__.py`
  - [ ] Create `buncker_fetch/` with `__init__.py`
  - [ ] Create `tests/` with `conftest.py`
  - [ ] Create `tests/shared/`, `tests/buncker/`, `tests/buncker_fetch/` (empty `__init__.py`)
  - [ ] Create `packaging/buncker/debian/` and `packaging/buncker-fetch/debian/` (empty dirs with `.gitkeep`)
- [ ] Task 2: Create `pyproject.toml` with ruff + pytest config (AC: 2)
  - [ ] Configure `[tool.ruff]` with `select = ["E", "F", "W", "I", "UP", "B", "SIM"]`, target-version `py311`
  - [ ] Configure `[tool.ruff.format]` defaults
  - [ ] Configure `[tool.pytest.ini_options]` with `testpaths = ["tests"]`
  - [ ] Add project metadata (name, version `0.0.1`, description, license Apache-2.0, requires-python `>=3.11`)
- [ ] Task 3: Verify and complete `.gitignore` (AC: 3)
  - [ ] Ensure entries: `__pycache__/`, `*.pyc`, `*.egg-info/`, `dist/`, `.pytest_cache/`, `.ruff_cache/`, `*.pyo`, `.eggs/`, `build/`
- [ ] Task 4: Create GitHub Actions CI workflow (AC: 4, 5)
  - [ ] Create `.github/workflows/ci.yml`
  - [ ] Job `lint`: `ruff check .` + `ruff format --check .`
  - [ ] Job `test`: `pytest` (depends on lint passing)
  - [ ] Trigger on `push` and `pull_request` to `main`
  - [ ] Use Python 3.11
  - [ ] Install dev dependencies: `pip install ruff pytest`
- [ ] Task 5: Create placeholder test (AC: 5)
  - [ ] Create `tests/test_placeholder.py` with a single passing test
  - [ ] Verify it passes with `pytest`
- [ ] Task 6: Create Makefile (AC: 6)
  - [ ] Target `lint`: runs `ruff check . && ruff format --check .`
  - [ ] Target `test`: runs `pytest`
  - [ ] Target `build`: placeholder echoing "build not yet implemented"
  - [ ] Add `.PHONY` declarations
- [ ] Task 7: Update `README.md` dev instructions (AC: 7)
  - [ ] Add "Development" section with setup instructions (clone, install ruff/pytest, make lint, make test)
  - [ ] Keep existing content, append dev section

## Dev Notes

### Source Tree

The complete target project structure is defined in the architecture. For this story, only the skeleton needs to be created (empty `__init__.py` files, placeholder dirs):

```
buncker/                    # Project root
  shared/
    __init__.py
  buncker/
    __init__.py
  buncker_fetch/
    __init__.py
  tests/
    conftest.py
    shared/
    buncker/
    buncker_fetch/
  packaging/
    buncker/debian/
    buncker-fetch/debian/
  pyproject.toml
  Makefile
  .github/workflows/ci.yml
```

[Source: `docs/architecture/10-source-tree.md`]

### Tech Stack (relevant to this story)

| Tool | Version | Purpose |
|------|---------|---------|
| Python | >=3.11 | Required minimum version |
| ruff | 0.8.x | Linting + formatting (replaces flake8+black+isort) |
| pytest | 8.x | Test framework |

[Source: `docs/architecture/3-tech-stack.md`]

### Coding Standards

- **Language:** Python >=3.11
- **Linting:** ruff with rules `E,F,W,I,UP,B,SIM`
- **Tests:** pytest, `tests/` directory mirrors source structure
- **No pip, no venv** constraint applies to production packages only; dev tools (ruff, pytest) are installed separately for development
- f-strings only, `pathlib.Path` for paths, type hints on public signatures

[Source: `docs/architecture/13-coding-standards.md`]

### CI Pipeline

CI order: `ruff check` -> `ruff format --check` -> `pytest (unit)` -> `pytest (integration)` -> `pytest (e2e)` -> `coverage`

For this story, only lint + unit test (placeholder) are needed. Integration and e2e stages will be added in later stories.

[Source: `docs/architecture/14-test-strategy.md`]

### Important Notes

- The `README.md`, `LICENSE`, `CONTRIBUTING.md`, `CHANGELOG.md`, `.gitignore`, and other root files already exist in the repo. This story only needs to add dev instructions to `README.md` and verify `.gitignore` completeness.
- The `.github/` directory already contains issue templates, CODEOWNERS, and PR template. The CI workflow file needs to be added.
- No existing code to build on - this is the first implementation story.

### Testing

- **Test location:** `tests/test_placeholder.py`
- **Framework:** pytest 8.x
- **This story only requires a single placeholder test** to verify CI works end-to-end
- The placeholder test should be simple (e.g., `assert True` or a basic import check)
- Future stories will add real tests following the `tests/` mirror structure

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_

## QA Results

_To be filled after QA review_
