# Story 3.4: Config, Setup & systemd

## Status

Done

## Story

**As an** operator,
**I want** to set up buncker with a single command and run it as a system service,
**so that** it's production-ready on my isolated LAN.

## Acceptance Criteria

1. `buncker setup` generates mnemonic, derives keys, creates config and store
2. `buncker serve` starts the HTTP daemon
3. CLI: `buncker {setup|serve|analyze|generate-manifest|import|status|gc|rotate-keys|export-ca}`
4. CLI commands call admin API locally (except `setup` and `serve`)
5. `rotate-keys` generates new mnemonic with configurable grace period
6. `export-ca` exports CA certificate if TLS enabled
7. Config loaded from `/etc/buncker/config.json` with sensible defaults
8. systemd unit with hardening is functional

## Tasks / Subtasks

- [x] Task 1: Implement config module (AC: 7)
  - [x] Create `buncker/config.py`
  - [x] Load config from `/etc/buncker/config.json` (or path override)
  - [x] Define defaults: `bind=127.0.0.1`, `port=5000`, `store_path=/var/lib/buncker`, `max_workers=16`, `tls=false`, `log_level=INFO`
  - [x] Validate config on load (raise `ConfigError` for invalid values)
- [x] Task 2: Implement `buncker setup` (AC: 1)
  - [x] Generate mnemonic via `crypto.generate_mnemonic()`
  - [x] Generate salt via `os.urandom(32)`
  - [x] Create config file with `source_id`, `mnemonic_hash` (SHA256 of mnemonic), `salt`
  - [x] Initialize store directory
  - [x] Display 16 words to operator (one-time display, never stored in cleartext)
- [x] Task 3: Implement `buncker serve` (AC: 2)
  - [x] Load config
  - [x] Read mnemonic from `BUNCKER_MNEMONIC` env var or prompt on stdin
  - [x] Derive keys in memory, verify mnemonic hash against config (exit on mismatch)
  - [x] Initialize store
  - [x] Start `BunckerServer` with config params and derived keys
  - [x] Handle SIGTERM/SIGINT for graceful shutdown
- [x] Task 4: Implement CLI entry point (AC: 3, 4)
  - [x] Create `buncker/__main__.py` with argparse subcommands
  - [x] Subcommands: `setup`, `serve`, `analyze`, `generate-manifest`, `import`, `status`, `gc`, `rotate-keys`, `export-ca`
  - [x] `setup` and `serve` run directly
  - [x] Other commands call admin API via `urllib.request` to `http://localhost:{port}/admin/...`
- [x] Task 5: Implement `rotate-keys` (AC: 5)
  - [x] Generate new mnemonic and salt
  - [x] Store old keys with `grace_period_days` (configurable, default 30)
  - [x] Update config with new crypto parameters
  - [x] Display new mnemonic to operator
- [x] Task 6: Implement `export-ca` (AC: 6)
  - [x] If TLS enabled in config, export CA certificate to stdout or file
  - [x] If TLS not enabled, inform operator
- [x] Task 7: Create systemd unit file (AC: 8)
  - [x] Create `packaging/buncker/debian/buncker.service`
  - [x] Include hardening: `NoNewPrivileges`, `ProtectSystem=strict`, `ProtectHome`, `ReadWritePaths`, `PrivateTmp`
  - [x] User: `buncker`, Group: `buncker`
  - [x] `Restart=on-failure`, `RestartSec=5`
- [x] Task 8: Write tests
  - [x] Test config loading with defaults and overrides
  - [x] Test config validation (invalid port, bad path, etc.)
  - [x] Test setup creates config and store
  - [x] Test CLI argument parsing
  - [x] Test CLI commands call correct admin API endpoints (mock HTTP)
  - [x] Test `rotate-keys` generates new mnemonic and updates config
  - [x] Test `export-ca` outputs certificate when TLS enabled, informs when not
  - [x] Test SIGTERM/SIGINT triggers graceful shutdown (skipped on Windows)

## Dev Notes

### Config File Format

```json
{
  "source_id": "buncker-prod-01",
  "bind": "127.0.0.1",
  "port": 5000,
  "store_path": "/var/lib/buncker",
  "max_workers": 16,
  "tls": false,
  "crypto": {"salt": "base64...", "mnemonic_hash": "sha256:..."},
  "private_registries": ["registry.internal", "localhost:*"],
  "gc": {"inactive_days_threshold": 90},
  "log_level": "INFO"
}
```

[Source: `docs/architecture/9-storage-schema.md`]

### systemd Unit

```ini
[Unit]
Description=Buncker - Offline Docker Registry
After=network.target

[Service]
Type=simple
User=buncker
Group=buncker
EnvironmentFile=/etc/buncker/env
ExecStart=/usr/bin/buncker serve
Restart=on-failure
RestartSec=5
WorkingDirectory=/var/lib/buncker
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=yes
ReadWritePaths=/var/lib/buncker /var/log/buncker
PrivateTmp=yes

[Install]
WantedBy=multi-user.target
```

[Source: `docs/architecture/11-infrastructure-and-deployment.md`]

### Secrets Management

- Mnemonic: displayed once during setup, never stored in cleartext
- Config stores only `mnemonic_hash` (SHA256) and `salt`
- Derived keys: in-memory only, derived once at daemon startup

**Secrets at runtime:** `buncker serve` needs the mnemonic to derive crypto keys. The daemon reads it via one of these methods (in priority order):
1. Environment variable `BUNCKER_MNEMONIC` (suitable for systemd `EnvironmentFile` or operator scripts)
2. Interactive prompt on stdin at startup (suitable for manual launch)

The daemon derives keys in memory at startup, verifies the mnemonic hash against config, and never writes keys or mnemonic to disk. If the mnemonic is wrong (hash mismatch), the daemon exits with an error.

For systemd, use `EnvironmentFile=/etc/buncker/env` with `BUNCKER_MNEMONIC="word1 word2 ..."` (file permissions 0600, owned by root:buncker).

[Source: `docs/architecture/15-security.md#Secrets Management`]

### Dev Environment

For development, the store path can be `/tmp/buncker-dev/` and config can be in the working directory.

[Source: `docs/architecture/11-infrastructure-and-deployment.md#Environments`]

### Dependencies on Previous Stories

- **Story 3.1:** BunckerServer
- **Story 3.2:** Admin API (for CLI proxy commands)
- **Story 3.3:** Transfer module
- **Story 1.2:** Crypto module
- **Story 1.4:** Exceptions (ConfigError) and logging

### Source Tree

```
buncker/
  __main__.py    # CLI entry point with argparse
  config.py      # Config loading and validation
packaging/
  buncker/debian/
    buncker.service  # systemd unit
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker/test_config.py` (new file, following mirror pattern)
- **Framework:** pytest 8.x with `tmp_path` and `unittest.mock`
- **Mock HTTP for CLI commands** testing admin API calls
- **Coverage:** 80% minimum

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |
| 2026-03-04 | 0.2 | QA fixes: resolve secrets at runtime (env var/stdin), add rotate-keys/export-ca/SIGTERM tests, EnvironmentFile in systemd | Romain G. |

## Dev Agent Record

### Agent Model Used

claude-opus-4-6

### Debug Log References

No issues encountered.

### Completion Notes List

- Config module with load/save/validate, sensible defaults, ConfigError on invalid values
- CLI with all 9 subcommands via argparse: setup, serve, analyze, generate-manifest, import, status, gc, rotate-keys, export-ca
- Setup generates mnemonic, salt, config file and initializes store
- Serve reads mnemonic from BUNCKER_MNEMONIC env or stdin, verifies hash, derives keys
- CLI proxy commands call admin API on localhost
- rotate-keys generates new mnemonic with configurable grace period, keeps previous keys in config
- export-ca checks TLS status and exports CA cert
- systemd unit with full hardening (NoNewPrivileges, ProtectSystem, ProtectHome, PrivateTmp)
- 21 tests (12 config + 9 CLI), all passing

### File List

- `buncker/config.py` (new) - Configuration loading and validation
- `buncker/__main__.py` (new) - CLI entry point with argparse
- `packaging/buncker/debian/buncker.service` (new) - systemd unit file
- `tests/buncker/test_config.py` (new) - Config tests
- `tests/buncker/test_main.py` (new) - CLI tests

## QA Results

### Review Date: 2026-03-04

### Reviewed By: Quinn (Test Architect)

4 findings: 1 high, 0 medium, 3 low.

### Gate Status

Gate: PASS - docs/qa/gates/3.4-config-setup-systemd.yml
