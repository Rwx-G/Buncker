# Story 2.1: Store Core

## Status

Draft

## Story

**As an** operator,
**I want** a reliable blob store on the filesystem,
**so that** imported Docker layers are safely stored and retrievable.

## Acceptance Criteria

1. `Store(path)` initializes an OCI Image Layout (`oci-layout`, `index.json`, `blobs/sha256/`, `meta/sha256/`)
2. `import_blob(data, expected_digest)` writes atomically (temp + verify SHA256 + rename). Raises `StoreError` on mismatch
3. `has_blob(digest)` returns bool
4. `get_blob(digest)` returns file path. Raises `StoreError` if absent
5. `list_missing(digests)` returns list of absent digests
6. Each import creates/updates the sidecar metadata JSON
7. `update_metadata(digest, event)` updates `last_requested` and increments `request_count`
8. Unit tests with `tmp_path`: import, idempotent re-import, corrupt blob rejected, missing list

## Tasks / Subtasks

- [ ] Task 1: Implement `Store.__init__()` (AC: 1)
  - [ ] Accept `path: Path` parameter
  - [ ] Create directory structure if not exists: `blobs/sha256/`, `meta/sha256/`
  - [ ] Write `oci-layout` file: `{"imageLayoutVersion": "1.0.0"}`
  - [ ] Write `index.json` if not exists: `{"schemaVersion": 2, "manifests": []}`
  - [ ] Idempotent: re-init on existing store is a no-op
- [ ] Task 2: Implement `import_blob()` (AC: 2, 6)
  - [ ] Write data to temp file in store directory
  - [ ] Compute SHA256 of written data
  - [ ] Compare with `expected_digest` - raise `StoreError` on mismatch, delete temp
  - [ ] Atomic rename to `blobs/sha256/{digest_hex}`
  - [ ] Create/update metadata sidecar in `meta/sha256/{digest_hex}.json`
  - [ ] Idempotent: if blob already exists with same digest, update metadata only
- [ ] Task 3: Implement `has_blob()` and `get_blob()` (AC: 3, 4)
  - [ ] `has_blob`: check `blobs/sha256/{digest_hex}` exists
  - [ ] `get_blob`: return `Path` to blob file, raise `StoreError` if absent
  - [ ] Handle `sha256:` prefix extraction from digest string
- [ ] Task 4: Implement `list_missing()` (AC: 5)
  - [ ] Accept list of digests, return those not present in store
- [ ] Task 5: Implement metadata sidecar management (AC: 6, 7)
  - [ ] Sidecar format: `{"digest": "...", "size": N, "media_type": "...", "image_refs": [], "first_imported": "ISO", "last_requested": "ISO", "request_count": 0, "gc_status": "active"}`
  - [ ] `update_metadata(digest, event)`: update `last_requested` timestamp, increment `request_count`
  - [ ] Atomic write for sidecar updates
- [ ] Task 6: Write unit tests (AC: 8)
  - [ ] Test store initialization creates correct structure
  - [ ] Test import_blob with valid data
  - [ ] Test import_blob idempotent re-import
  - [ ] Test import_blob with SHA256 mismatch raises `StoreError`
  - [ ] Test has_blob returns correct bool
  - [ ] Test get_blob returns path / raises StoreError
  - [ ] Test list_missing returns correct subset
  - [ ] Test metadata sidecar created and updated correctly

## Dev Notes

### Data Model - OCI Blob Metadata

Each blob has a JSON sidecar at `meta/sha256/{digest_hex}.json`:

```json
{
  "digest": "sha256:abc123...",
  "size": 12345,
  "media_type": "application/vnd.oci.image.layer.v1.tar+gzip",
  "image_refs": ["docker.io/library/nginx:1.25"],
  "first_imported": "2026-03-04T10:00:00Z",
  "last_requested": "2026-03-04T12:00:00Z",
  "request_count": 3,
  "gc_status": "active"
}
```

[Source: `docs/architecture/4-data-models.md#OCI Blob`]

### Storage Schema

```
/var/lib/buncker/
  oci-layout
  index.json
  blobs/sha256/{digest_hex}
  meta/sha256/{digest_hex}.json
  manifests/{registry}/{repo}/{tag}/{platform}.json
  logs/buncker.jsonl
```

For tests, use `tmp_path` as the store root.

[Source: `docs/architecture/9-storage-schema.md`]

### Critical Rules

- **Atomic writes only:** temp file + verify SHA256 + `os.rename`. Never corrupt the store.
- **SHA256 verify on every blob read/write:** No exceptions, no skip.
- **Idempotent:** Importing the same blob twice = noop (same digest = same file).

[Source: `docs/architecture/13-coding-standards.md`, `docs/architecture/12-error-handling-strategy.md`]

### Dependencies on Previous Stories

- **Story 1.1:** Project structure exists
- **Story 1.4:** `StoreError` exception from `shared/exceptions.py`
- **Story 1.3:** OCI dataclasses from `shared/oci.py` (for `oci-layout` and `index.json` format awareness)

### Source Tree

```
buncker/
  store.py         # Store class
tests/
  buncker/
    test_store.py  # Store tests
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker/test_store.py`
- **Framework:** pytest 8.x with `tmp_path` fixture
- **Pattern:** Each test creates a fresh store in `tmp_path`
- **Coverage:** 80% minimum
- Use real SHA256 digests computed from test data

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_

## QA Results

_To be filled after QA review_
