# Story 2.2: Store GC

## Status

Done

## Story

**As an** operator,
**I want** to identify and clean up unused blobs,
**so that** disk space is managed without risking active images.

## Acceptance Criteria

1. `gc_report(inactive_days)` scans meta sidecars and returns candidates list
2. Shared blob is candidate only if its `last_requested` is older than `inactive_days` (blob-level proxy - see Dev Notes)
3. `gc_execute(digests, operator)` deletes blobs and sidecars, logs each deletion
4. `gc_execute` refuses to delete a digest not in the latest report
5. Tests: active blob not candidate, shared blob protected, effective deletion, log produced

## Tasks / Subtasks

- [x] Task 1: Implement `gc_report()` (AC: 1, 2)
  - [x] Scan all sidecar files in `meta/sha256/`
  - [x] A blob is a candidate if `last_requested` is older than `inactive_days` days
  - [x] Shared blob (multiple `image_refs`) is candidate based on `last_requested` age (blob-level, not per-ref)
  - [x] Return list of candidate dicts: `{"digest": ..., "size": ..., "last_requested": ..., "image_refs": [...]}`
  - [x] Store report internally for validation by `gc_execute`
- [x] Task 2: Implement `gc_execute()` (AC: 3, 4)
  - [x] Accept list of digests + operator name (for audit log)
  - [x] Validate each digest is in the latest report - refuse if not
  - [x] Delete blob file from `blobs/sha256/`
  - [x] Delete sidecar from `meta/sha256/`
  - [x] Log each deletion with event `gc_executed` including digest, size, operator
  - [x] Return summary: count deleted, bytes freed
- [x] Task 3: Write tests (AC: 5)
  - [x] Test active blob (recently requested) is NOT a candidate
  - [x] Test inactive blob is a candidate
  - [x] Test shared blob with recent `last_requested` is protected (regardless of ref count)
  - [x] Test shared blob with old `last_requested` is a candidate
  - [x] Test gc_execute deletes blob and sidecar
  - [x] Test gc_execute refuses digest not in report
  - [x] Test gc_execute produces log entries

## Dev Notes

### GC Design

GC is **manual only** - never automatic deletion. The operator runs a report first, reviews, then explicitly executes. This is a safety feature.

[Source: `docs/architecture/5-components.md#buncker/store` - "GC is admin-triggered, never automatic"]

Flow:
1. `gc_report(inactive_days)` -> list of candidates
2. Operator reviews candidates
3. `gc_execute(digests, operator)` -> deletions

The report is stored in memory (instance attribute) and must match the execute call. If a new report is generated, the previous one is invalidated. This prevents stale reports from being executed after store state has changed.

[Source: `docs/architecture/5-components.md#buncker/store` - report-then-execute pattern is the GC contract]

### Blob Activity Model

The data model (`docs/architecture/4-data-models.md`) defines a single `last_requested` timestamp per blob (not per `image_ref`). This means GC activity is determined at the blob level: if any client has pulled the blob recently (updating `last_requested`), the blob is considered active regardless of how many images reference it. The `image_refs` list is informational for the GC report but does not drive the activity decision.

### Logging Events

- Event `gc_candidate`: logged during report generation
- Event `gc_executed`: logged during deletion with `digest`, `size`, `operator`
- Use the JSON Lines logger from `shared/logging.py` (Story 1.4)

[Source: `docs/architecture/12-error-handling-strategy.md#Logging`]

### Dependencies on Previous Stories

- **Story 2.1:** Store core must exist (import_blob, has_blob, metadata sidecars)
- **Story 1.4:** Logger and `StoreError` exception

### Source Tree

GC methods are part of the `Store` class in `buncker/store.py`. Tests in `tests/buncker/test_store.py` (same file as Store core tests).

> **Note:** The source tree (`docs/architecture/10-source-tree.md`) only lists `tests/buncker/test_store.py`. There is no separate `test_store_gc.py` - GC tests go in the existing test file.

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker/test_store.py`
- **Framework:** pytest 8.x with `tmp_path`
- **Setup:** Create store, import blobs with known dates, manipulate `last_requested` in sidecars to simulate age
- **Coverage:** 80% minimum

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |
| 2026-03-04 | 0.2 | QA fixes: clarify blob-level last_requested proxy, remove test_store_gc.py alternative, source GC design decisions | Romain G. |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

None

### Completion Notes List

- GC methods added to existing Store class in buncker/store.py
- 9 new tests (30 total in test_store.py)
- Uses `_last_gc_report` instance attribute for report-then-execute validation

### File List

- `buncker/store.py` (modified - added gc_report, gc_execute)
- `tests/buncker/test_store.py` (modified - added GC tests)

## QA Results

_To be filled after QA review_
