# Story 1.2: Crypto Module

## Status

Ready for Review

## Story

**As an** operator,
**I want** the cryptographic primitives implemented and tested,
**so that** all transfer security relies on proven, audited code.

## Acceptance Criteria

1. `generate_mnemonic()` returns 12 words from BIP-39 wordlist (2048 words) with `secrets.token_bytes` entropy
2. `derive_keys(mnemonic, salt, iterations=600_000)` returns tuple `(aes_key, hmac_key)` via PBKDF2-SHA256
3. `encrypt(data, aes_key)` encrypts with AES-256-GCM and returns nonce + ciphertext + tag
4. `decrypt(data, aes_key)` decrypts and verifies auth tag. Raises `CryptoError` if invalid
5. `sign(data, hmac_key)` returns HMAC-SHA256 hex digest
6. `verify(data, hmac_key, signature)` returns bool (constant-time comparison)
7. `shared/wordlist.py` contains the complete BIP-39 wordlist (2048 words) embedded
8. Unit tests: round-trip encrypt/decrypt, wrong key -> CryptoError, valid/invalid HMAC, mnemonic has 12 valid words
9. 100% coverage on this module

## Tasks / Subtasks

- [x] Task 1: Create BIP-39 wordlist file (AC: 7)
  - [x] Create `shared/wordlist.py` with the complete 2048-word BIP-39 English wordlist as a tuple
  - [x] Add a simple validation: `assert len(WORDLIST) == 2048`
- [x] Task 2: Implement `generate_mnemonic()` (AC: 1)
  - [x] Use `secrets.token_bytes(16)` for 128-bit entropy
  - [x] Convert entropy to 12 word indices from BIP-39 wordlist
  - [x] Return space-separated string of 12 words
- [x] Task 3: Implement `derive_keys()` (AC: 2)
  - [x] Use `cryptography.hazmat.primitives.kdf.pbkdf2.PBKDF2HMAC` with SHA256
  - [x] 600,000 iterations default, derive 64 bytes, split into `aes_key` (32 bytes) + `hmac_key` (32 bytes)
  - [x] Accept mnemonic as string, encode to bytes internally
- [x] Task 4: Implement `encrypt()` and `decrypt()` (AC: 3, 4)
  - [x] Use `cryptography.hazmat.primitives.ciphers.aead.AESGCM`
  - [x] `encrypt`: generate 12-byte nonce via `os.urandom`, return `nonce + ciphertext_with_tag`
  - [x] `decrypt`: extract nonce (first 12 bytes), decrypt remainder, raise `CryptoError` on failure
- [x] Task 5: Implement `sign()` and `verify()` (AC: 5, 6)
  - [x] `sign`: `hmac.new(hmac_key, data, hashlib.sha256).hexdigest()`
  - [x] `verify`: `hmac.compare_digest()` for constant-time comparison
- [x] Task 6: Write comprehensive unit tests (AC: 8, 9)
  - [x] Test `generate_mnemonic`: returns 12 words, all words in wordlist, two calls differ
  - [x] Test `derive_keys`: deterministic (same input = same output), returns 32-byte keys
  - [x] Test encrypt/decrypt round-trip with various data sizes
  - [x] Test decrypt with wrong key raises `CryptoError`
  - [x] Test decrypt with tampered ciphertext raises `CryptoError`
  - [x] Test sign/verify round-trip
  - [x] Test verify with wrong signature returns False
  - [x] Test verify with wrong key returns False
  - [x] Verify 100% coverage on `shared/crypto.py` and `shared/wordlist.py`

## Dev Notes

### Crypto Architecture

The crypto module is in `shared/crypto.py` and is used by both `buncker` and `buncker-fetch`. It is the only module that depends on an external package (`python3-cryptography`).

[Source: `docs/architecture/5-components.md#shared/crypto`]

### Implementation Details

**AES-256-GCM:**
- Library: `cryptography.hazmat.primitives.ciphers.aead.AESGCM`
- Nonce: 12 bytes from `os.urandom`
- Wire format: `nonce (12 bytes) || ciphertext || tag (16 bytes)` - GCM appends tag automatically

**PBKDF2:**
- Library: `cryptography.hazmat.primitives.kdf.pbkdf2.PBKDF2HMAC`
- Hash: SHA256
- Iterations: 600,000 (default, configurable)
- Output: 64 bytes total, split into two 32-byte keys (AES key + HMAC key)

**HMAC:**
- stdlib `hmac` + `hashlib.sha256`
- Output: hex digest string
- Comparison: `hmac.compare_digest()` (constant-time)

**BIP-39 Mnemonic:**
- Entropy: `secrets.token_bytes(16)` (128 bits)
- Wordlist: 2048 English words (standard BIP-39)
- Output: 12 space-separated words

[Source: `docs/architecture/15-security.md`, `docs/architecture/5-components.md#shared/crypto`]

### Security Rules

- Never log mnemonic, derived keys, or tokens
- Derived keys are in-memory only during execution, never written to disk
- Config stores only verification hashes and salts
- Mnemonic communicated once via human channel (paper)

[Source: `docs/architecture/15-security.md#Secrets Management`]

### Exception Handling

`CryptoError(BunckerError)` is the exception class for all crypto failures. It accepts a message string and optional context dict. Must be defined in `shared/exceptions.py` (Story 1.4), but can be created inline or imported if 1.4 is done first.

[Source: `docs/architecture/12-error-handling-strategy.md`]

### Source Tree

```
shared/
  __init__.py
  crypto.py       # All crypto functions
  wordlist.py     # BIP-39 2048-word list
tests/
  shared/
    test_crypto.py  # All crypto tests
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/shared/test_crypto.py`
- **Framework:** pytest 8.x, `unittest.mock` for mocking if needed
- **Coverage requirement:** 100% on this module (critical security code)
- **Test philosophy:** Test-after, but given this is crypto, be thorough
- **Pattern:** Each public function gets its own test class or group
- No mocking of crypto primitives - test real encrypt/decrypt round-trips

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

- `tests/shared/__init__.py` namespace collision with `shared/` package - resolved by removing `__init__.py` from test subdirectories and using `--import-mode=importlib` in pytest config
- Ruff import sorting: `cryptography.hazmat.primitives` imports needed alphabetical order

### Completion Notes List

- All 6 tasks completed, 21 crypto tests + 1 placeholder = 22 total passing
- 100% coverage on shared/crypto.py and shared/wordlist.py
- CryptoError defined inline in crypto.py (Story 1.4 will create shared/exceptions.py)
- BIP-39 mnemonic uses proper checksum (SHA256 first 4 bits appended to 128-bit entropy)
- Removed `__init__.py` from test subdirs to avoid namespace collision; added `pythonpath` and `--import-mode=importlib` to pyproject.toml

### File List

- `shared/wordlist.py` (new) - BIP-39 2048-word English wordlist
- `shared/crypto.py` (new) - All crypto functions + CryptoError
- `tests/shared/test_crypto.py` (new) - 21 comprehensive crypto tests
- `pyproject.toml` (modified) - Added pythonpath and import-mode config
- `tests/shared/__init__.py` (deleted) - Namespace collision fix
- `tests/buncker/__init__.py` (deleted) - Namespace collision fix
- `tests/buncker_fetch/__init__.py` (deleted) - Namespace collision fix

## QA Results

_To be filled after QA review_
