# Story 6.3: Audit Trail Enrichment

## Status

Done

## Story

**As an** operator,
**I want** to see who accessed the API and from where,
**so that** I have full traceability of remote operations.

## Acceptance Criteria

1. All API log entries include `client_ip` (source IP of the request)
2. All API log entries include `auth_level`: `admin`, `readonly`, `local`, or `rejected`
3. All API log entries include `user_agent` (User-Agent header value)
4. Local CLI requests (via localhost) are logged with `auth_level: local`
5. Failed authentication attempts are logged with `auth_level: rejected`, `client_ip`, and `user_agent`
6. No token values are ever logged (existing NFR7 principle)
7. Tests: verify log entries contain new fields for each auth level scenario

## Tasks / Subtasks

- [x] Task 1: Extract request metadata in handler (AC: 1, 2, 3)
  - [x] Extract `client_ip` from `self.client_address[0]`
  - [x] Extract `user_agent` from `User-Agent` header (default `""`)
  - [x] Determine `auth_level` from auth middleware result or `local` for localhost
- [x] Task 2: Enrich log entries (AC: 1, 2, 3, 4)
  - [x] Add `client_ip`, `auth_level`, `user_agent` fields to all existing log calls in handler
  - [x] Ensure localhost detection works for both `127.0.0.1` and `::1`
- [x] Task 3: Log failed auth attempts (AC: 5, 6)
  - [x] Log with event `api_auth_rejected` and `auth_level: rejected`
  - [x] Include `client_ip` and `user_agent` but NEVER the token value
- [x] Task 4: Write tests (AC: 7)
  - [x] Test log entry contains `client_ip` for remote request
  - [x] Test log entry contains `auth_level: rejected` for failed auth
  - [x] Test `user_agent` captured correctly
  - [x] Verify no token values appear in logs

## Dev Notes

### Log Entry Example

```json
{
  "ts": "2026-03-06T10:30:00Z",
  "event": "dockerfile_analyzed",
  "level": "INFO",
  "client_ip": "192.168.1.42",
  "auth_level": "admin",
  "user_agent": "curl/8.5.0",
  "images": 3,
  "missing_blobs": 12
}
```

[Source: `docs/architecture/12-error-handling-strategy.md`]

### New Log Events

- `api_auth_rejected` - failed authentication attempt

[Source: `docs/architecture/12-error-handling-strategy.md`]

### Dependencies on Previous Stories

- **Story 6.2:** Auth middleware (provides auth_level)
- **Story 1.4:** Logging setup (JSON Lines logger)
- **Story 3.2:** Admin API (existing log calls to enrich)

### Source Tree

```
buncker/
  handler.py    # Modified - enrich log calls
  auth.py       # Modified - log rejected attempts
tests/
  buncker/
    test_auth.py  # Extended with audit log tests
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker/test_auth.py`
- **Framework:** pytest 8.x
- **Pattern:** Capture log output and verify JSON fields
- **Coverage:** 80% minimum

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 0.1 | Initial draft from elicitation | Romain G. |
