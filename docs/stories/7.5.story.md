# Story 7.5: Manifest Cache TTL & Staleness

## Status

Draft

## Story

**As an** operator,
**I want** to know when cached manifests are outdated,
**so that** I can re-fetch them to stay in sync with upstream registries.

## Acceptance Criteria

1. `manifest_ttl` config option (integer, days, default 30) in `config.json`
2. `buncker analyze` emits a warning for each manifest whose `_buncker.cached_at` is older than `manifest_ttl` days
3. Warning format: `"Manifest for {image}:{tag} is {N} days old (TTL: {ttl}d) - consider using --refresh-stale"`
4. `buncker generate-manifest --refresh-stale` includes stale manifest digests in the transfer request with a `refresh: true` flag
5. `buncker-fetch` re-downloads manifests flagged with `refresh: true` even if already in local cache
6. Updated manifests are included in `response.tar.enc` and imported back into the offline manifest cache
7. `GET /admin/status` includes `stale_manifests` count in the response
8. Tests: fresh manifest no warning, stale manifest triggers warning, --refresh-stale includes stale in request, buncker-fetch re-fetches flagged manifests

## Tasks / Subtasks

- [ ] Task 1: Add config option (AC: 1)
  - [ ] Add `manifest_ttl` field to config schema (integer, default 30)
  - [ ] Validate: must be positive integer
  - [ ] Document in config reference
- [ ] Task 2: Staleness detection in analyze (AC: 2, 3)
  - [ ] Add `is_stale()` method to `ManifestCache` in `registry_client.py`
  - [ ] Compare `_buncker.cached_at` timestamp against current time minus `manifest_ttl` days
  - [ ] Emit warning in `resolve_dockerfile()` for stale manifests
  - [ ] Collect stale manifest list in `AnalysisResult` (new field: `stale_manifests`)
- [ ] Task 3: Refresh-stale in generate-manifest (AC: 4)
  - [ ] Add `--refresh-stale` flag to `buncker generate-manifest` CLI
  - [ ] When flag is set, include stale manifest references in transfer request JSON
  - [ ] Add `refresh: true` flag per manifest entry in the request
  - [ ] Admin API: `POST /admin/generate-manifest` accepts `refresh_stale: true` parameter
- [ ] Task 4: buncker-fetch manifest re-download (AC: 5, 6)
  - [ ] In `buncker_fetch/transfer.py`: detect `refresh: true` flag on manifest entries
  - [ ] Re-download manifest from registry even if local cache has it
  - [ ] Include updated manifests in `response.tar.enc`
- [ ] Task 5: Import updated manifests (AC: 6)
  - [ ] `buncker import` updates manifest cache with fresh manifests from response
  - [ ] Update `_buncker.cached_at` to current timestamp on re-imported manifests
- [ ] Task 6: Status endpoint (AC: 7)
  - [ ] Add `stale_manifests` count to `GET /admin/status` response
  - [ ] Count manifests where `cached_at` + `manifest_ttl` < now
- [ ] Task 7: Write tests (AC: 8)
  - [ ] Test fresh manifest (cached_at = today): no warning emitted
  - [ ] Test stale manifest (cached_at = 45 days ago, TTL=30): warning emitted
  - [ ] Test --refresh-stale includes stale manifests in request
  - [ ] Test buncker-fetch re-downloads manifest with `refresh: true`
  - [ ] Test imported manifest updates `cached_at`
  - [ ] Test `/admin/status` returns correct `stale_manifests` count

## Dev Notes

### Transfer Request Schema Extension

```json
{
  "version": 1,
  "buncker_version": "1.0.0",
  "source_id": "abc123",
  "blobs": [...],
  "manifests": [
    {
      "registry": "docker.io",
      "repository": "library/nginx",
      "tag": "1.25",
      "platform": "linux/amd64",
      "refresh": true
    }
  ]
}
```

The `manifests` array is new - previous versions only had `blobs`. `buncker-fetch` must handle requests both with and without `manifests` (backward compat).

### ManifestCache.is_stale()

```python
def is_stale(self, registry: str, repository: str, tag: str,
             platform: str, ttl_days: int) -> bool | None:
    """Check if a cached manifest exceeds the TTL.

    Returns None if manifest not cached, True if stale, False if fresh.
    """
```

### Dependencies on Previous Stories

- **Story 2.3:** Manifest cache (`ManifestCache`, `_buncker.cached_at`)
- **Story 2.5:** Resolver pipeline (`resolve_dockerfile()`, `AnalysisResult`)
- **Story 3.3:** Transfer generate/import
- **Story 4.3:** buncker-fetch transfer processing

### Source Tree

```
buncker/
  registry_client.py  # Add is_stale() to ManifestCache
  resolver.py          # Add stale warnings, stale_manifests field
  transfer.py          # Add manifest refresh entries to request
  handler.py           # Add stale_manifests to /admin/status
  config.py            # Add manifest_ttl config
  __main__.py          # Add --refresh-stale flag
buncker_fetch/
  transfer.py          # Handle refresh: true manifests
tests/
  buncker/
    test_registry_client.py  # Add staleness tests
    test_resolver.py         # Add stale warning tests
    test_transfer.py         # Add refresh-stale request tests
  buncker_fetch/
    test_transfer.py         # Add manifest re-download tests
```

### Testing

- **Test locations:** Multiple test files (see source tree)
- **Framework:** pytest 8.x
- Use `freezegun` or manual datetime mocking for TTL calculations
- **Backward compat:** Verify requests without `manifests` array still work

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-09 | 0.1 | Initial draft from elicitation | Romain G. |
