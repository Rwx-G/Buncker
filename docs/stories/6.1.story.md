# Story 6.1: Token Generation & Storage

## Status

Draft

## Story

**As an** operator,
**I want** `buncker api-setup` to generate authentication tokens and activate TLS,
**so that** I can securely open the admin API to LAN clients.

## Acceptance Criteria

1. `buncker api-setup` generates two cryptographically random Bearer tokens (256-bit via `secrets.token_hex(32)`): one read-only, one admin
2. Tokens are stored in `/etc/buncker/api-tokens.json` with file permissions 0600
3. `api-setup` prompts for a TLS certificate (`--cert`, `--key` flags) or generates an auto-signed certificate with an explicit security warning
4. TLS is activated on the daemon (HTTPS on same port 5000)
5. The existing `buncker export-ca` mechanism is reused for auto-signed certificates
6. Config is updated with `api.enabled: true` and `tls: true`
7. The daemon refuses to start if `api.enabled: true` and `tls: false`
8. Both tokens are displayed once at setup, clearly labeled (read-only vs admin)
9. Tests: token file created with correct permissions, config updated, TLS activated

## Tasks / Subtasks

- [ ] Task 1: Add `api-setup` subcommand to CLI (AC: 1, 3, 8)
  - [ ] Register `api-setup` in `__main__.py` argparse
  - [ ] Add `--cert` and `--key` optional flags
  - [ ] Generate two 256-bit tokens via `secrets.token_hex(32)`
  - [ ] Display tokens clearly labeled (read-only / admin)
- [ ] Task 2: Implement token persistence (AC: 2)
  - [ ] Write tokens to `/etc/buncker/api-tokens.json`
  - [ ] Set file permissions to 0600
  - [ ] Add `load_api_tokens()` to `config.py`
- [ ] Task 3: TLS activation logic (AC: 3, 4, 5)
  - [ ] If `--cert` and `--key` provided: validate and configure TLS with provided cert
  - [ ] If not provided: generate auto-signed cert + CA (reuse existing mechanism)
  - [ ] Display explicit security warning for auto-signed certificates
  - [ ] Reuse `buncker export-ca` for auto-signed CA distribution
- [ ] Task 4: Config update (AC: 6, 7)
  - [ ] Set `api.enabled: true` and `tls: true` in config.json
  - [ ] Add startup check: refuse to start if `api.enabled: true` and `tls: false`
- [ ] Task 5: Write tests (AC: 9)
  - [ ] Test token generation produces valid 256-bit hex strings
  - [ ] Test token file created with correct permissions (mock os.chmod on non-Linux)
  - [ ] Test config updated with `api.enabled` and `tls` fields
  - [ ] Test startup refusal when api.enabled=true and tls=false
  - [ ] Test with provided cert vs auto-signed

## Dev Notes

### Token Storage Schema

```json
// /etc/buncker/api-tokens.json (mode 0600)
{
  "readonly": "hex-encoded-256-bit-token",
  "admin": "hex-encoded-256-bit-token"
}
```

[Source: `docs/architecture/9-storage-schema.md`]

### Config Changes

New `api` section in `/etc/buncker/config.json`:

```json
{
  "api": { "enabled": false }
}
```

After `api-setup`: `api.enabled: true`, `tls: true`.

[Source: `docs/architecture/9-storage-schema.md`]

### Dependencies on Previous Stories

- **Story 3.4:** Config, setup & systemd (config loading, CLI infrastructure)
- **Story 1.2:** Crypto module (TLS cert generation uses `python3-cryptography`)

### Source Tree

```
buncker/
  __main__.py   # Add api-setup subcommand
  config.py     # Add load_api_tokens(), api config fields
  auth.py       # New module (token loading used in Story 6.2)
tests/
  buncker/
    test_auth.py  # New test file
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker/test_auth.py`
- **Framework:** pytest 8.x
- **Coverage:** 80% minimum
- Use `tmp_path` for config/token file creation

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 0.1 | Initial draft from elicitation | Romain G. |
