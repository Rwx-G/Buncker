# Story 4.4: CLI buncker-fetch

## Status

Draft

## Story

**As an** operator,
**I want** a complete CLI to manage the online side,
**so that** I can pair, inspect, fetch, and manage the cache.

## Acceptance Criteria

1. `buncker-fetch pair`: enter 12 words, derive keys, save config
2. `buncker-fetch inspect`: decrypt, display summary
3. `buncker-fetch fetch`: full cycle with `--output` and `--parallelism` options
4. `buncker-fetch status`: cache state
5. `buncker-fetch cache clean`: cleanup
6. Human-readable output by default, `--json` for machine output
7. All errors actionable with context
8. Tests: each subcommand tested

## Tasks / Subtasks

- [ ] Task 1: Implement config module (AC: 1)
  - [ ] Create `buncker_fetch/config.py`
  - [ ] Load/save config from `~/.buncker/config.json`
  - [ ] Config fields: `derived_key_check`, `salt`, `registries`
  - [ ] Validate config on load
- [ ] Task 2: Implement `pair` subcommand (AC: 1)
  - [ ] Prompt operator for 12 BIP-39 words
  - [ ] Validate all words exist in wordlist
  - [ ] Derive keys with `crypto.derive_keys(mnemonic, salt)`
  - [ ] Save config with `derived_key_check` (encrypted marker to verify correct mnemonic)
  - [ ] Display "Pairing successful"
- [ ] Task 3: Implement `inspect` subcommand (AC: 2)
  - [ ] Accept `request.json.enc` file path
  - [ ] Decrypt and verify HMAC
  - [ ] Display summary: source_id, generated_at, blob count, total size, registries involved
- [ ] Task 4: Implement `fetch` subcommand (AC: 3)
  - [ ] Accept `request.json.enc` file path
  - [ ] Options: `--output` (response path), `--parallelism N` (default 4)
  - [ ] Full cycle: process_request -> fetch blobs -> build_response
  - [ ] Display progress and final summary
- [ ] Task 5: Implement `status` subcommand (AC: 4)
  - [ ] Display cache stats: blob count, total size, oldest/newest blob
- [ ] Task 6: Implement `cache clean` subcommand (AC: 5)
  - [ ] Accept `--older-than Nd` (e.g., `30d`)
  - [ ] Delete old cached blobs
  - [ ] Display count and bytes freed
- [ ] Task 7: Implement output formatting (AC: 6)
  - [ ] Default: human-readable tables/text to stdout
  - [ ] `--json` flag: structured JSON output
  - [ ] Errors to stderr always
- [ ] Task 8: Create CLI entry point (AC: 7)
  - [ ] Create `buncker_fetch/__main__.py` with argparse subcommands
  - [ ] Subcommands: `pair`, `inspect`, `fetch`, `status`, `cache clean`
  - [ ] All errors display actionable messages (what failed + what to do)
- [ ] Task 9: Write tests (AC: 8)
  - [ ] Test pair with valid/invalid mnemonic
  - [ ] Test inspect displays correct summary
  - [ ] Test fetch orchestration (mock registry + cache)
  - [ ] Test status output
  - [ ] Test cache clean
  - [ ] Test --json flag produces valid JSON
  - [ ] Test error messages are actionable

## Dev Notes

### CLI Commands Reference

| Command | Description |
|---------|-------------|
| `buncker-fetch pair` | Enter 12 words, derive keys, save config |
| `buncker-fetch inspect <file>` | Decrypt request, show summary |
| `buncker-fetch fetch <file> [--output PATH] [--parallelism N]` | Full cycle |
| `buncker-fetch status` | Cache stats |
| `buncker-fetch cache clean [--older-than Nd]` | Cleanup |

[Source: `docs/architecture/5-components.md#buncker-fetch`]

### Online Config Format

```json
{
  "derived_key_check": "encrypted_marker_base64",
  "salt": "base64...",
  "registries": {
    "docker.io": {"user_env": "BUNCKER_DOCKER_USER", "pass_env": "BUNCKER_DOCKER_PASS"},
    "ghcr.io": {"user_env": "BUNCKER_GHCR_USER", "pass_env": "BUNCKER_GHCR_PASS"}
  }
}
```

Credentials stored as env var references, never plaintext.

[Source: `docs/architecture/9-storage-schema.md`, `docs/architecture/15-security.md`]

### Pairing Workflow

```
Operator (offline): buncker setup -> displays 12 words
Human channel: operator writes words on paper
Operator (online): buncker-fetch pair -> enters 12 words -> keys derived
```

[Source: `docs/architecture/7-core-workflows.md#Workflow 5`]

### Error Handling

All errors must be actionable:
- "Pairing failed: word 'xyz' not in BIP-39 wordlist. Check spelling."
- "Decryption failed: wrong key or corrupted file. Re-pair if needed."
- "Registry error: HTTP 429 from docker.io. Rate limited, retry in 5 minutes."

[Source: `docs/architecture/12-error-handling-strategy.md`]

### Dependencies on Previous Stories

- **Story 4.1:** Registry client
- **Story 4.2:** Fetcher and cache
- **Story 4.3:** Transfer (process_request, build_response)
- **Story 1.2:** Crypto module (derive_keys)
- **Story 1.4:** Exceptions and logging

### Source Tree

```
buncker_fetch/
  __main__.py    # CLI entry point
  config.py      # Config loading
tests/
  buncker_fetch/
    test_cli.py  # CLI tests (new file, following pattern)
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker_fetch/test_cli.py` (or per-subcommand files)
- **Framework:** pytest 8.x with `tmp_path` and `unittest.mock`
- **Pattern:** Mock underlying modules, test CLI argument parsing and orchestration
- **Coverage:** 80% minimum

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_

## QA Results

_To be filled after QA review_
