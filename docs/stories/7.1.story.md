# Story 7.1: Docker Compose Analysis

## Status

Draft

## Story

**As an** operator,
**I want** to analyze a `docker-compose.yml` file to extract all image references,
**so that** I can synchronize all images needed for a multi-service project in one operation.

## Acceptance Criteria

1. `buncker analyze --compose docker-compose.yml` parses the YAML file and extracts `image:` fields from all services
2. Services with `build.dockerfile` have their Dockerfile resolved through the existing resolver pipeline
3. Services with both `image:` and `build:` use the `image:` reference (same as Docker Compose behavior for pre-built images)
4. Services with only `build.context` (no explicit `dockerfile`) default to `Dockerfile` in the build context directory
5. The Compose file is validated: missing `services:` key or empty services returns an actionable error
6. `POST /admin/analyze` accepts `compose_content` (string) for remote clients alongside existing `dockerfile_content`
7. The analysis result aggregates all images with deduplication (same image used by multiple services counted once)
8. `python3-yaml` dependency added to .deb control files
9. Tests: multi-service Compose file, mixed image+build services, deduplication, invalid YAML, remote content mode

## Tasks / Subtasks

- [ ] Task 1: Add Compose parser module (AC: 1, 2, 3, 4, 5)
  - [ ] Create `buncker/compose.py` with `parse_compose()` function
  - [ ] Extract `image:` fields from all `services:` entries
  - [ ] Extract `build.dockerfile` paths and resolve relative to `build.context`
  - [ ] Handle `image:` + `build:` coexistence (prefer `image:`)
  - [ ] Handle `build.context` without explicit `dockerfile` (default to `Dockerfile`)
  - [ ] Validate Compose structure: `services:` key required, non-empty
  - [ ] Return list of `ComposeService` dataclass (name, image_ref, dockerfile_path)
- [ ] Task 2: Integrate with resolver pipeline (AC: 1, 7)
  - [ ] Add `resolve_compose()` function that calls `resolve_dockerfile()` for each Dockerfile
  - [ ] Aggregate `AnalysisResult` across all services with blob deduplication
  - [ ] Add `--compose` flag to CLI argparse in `__main__.py`
- [ ] Task 3: Admin API integration (AC: 6)
  - [ ] Add `compose_content` field support in `POST /admin/analyze`
  - [ ] Parse YAML from string content for remote clients
  - [ ] Reject `compose_path` from remote clients (same pattern as `dockerfile_path`)
- [ ] Task 4: Update packaging (AC: 8)
  - [ ] Add `python3-yaml` to .deb `Depends` in both control files
  - [ ] Verify `python3-pyyaml` package name for RPM (Story 7.2)
- [ ] Task 5: Write tests (AC: 9)
  - [ ] Test multi-service Compose with mixed `image:` and `build:` services
  - [ ] Test deduplication (same image in multiple services)
  - [ ] Test invalid YAML returns actionable error
  - [ ] Test missing `services:` key returns error
  - [ ] Test remote `compose_content` mode via admin API
  - [ ] Test `build.context` without `dockerfile` defaults correctly

## Dev Notes

### Compose Parser Design

```python
# buncker/compose.py
@dataclass
class ComposeService:
    name: str
    image_ref: str | None       # From image: field
    dockerfile_path: Path | None # From build.dockerfile or build.context/Dockerfile
    build_context: Path | None   # From build.context

def parse_compose(path: Path) -> list[ComposeService]:
    """Parse docker-compose.yml and extract image references."""
```

Uses `yaml.safe_load()` - no custom constructors needed.

### Priority rules for image resolution

1. `image:` present -> use as direct image reference (resolver treats it as FROM)
2. `build.dockerfile` present (no `image:`) -> resolve the Dockerfile
3. `build.context` only (no `image:`, no `dockerfile`) -> resolve `{context}/Dockerfile`
4. Neither `image:` nor `build:` -> skip with warning

### New dependency

`python3-yaml` (Debian package) / `python3-pyyaml` (RPM package) - provides `yaml` module.

### Dependencies on Previous Stories

- **Story 2.4:** Dockerfile parser (`parse_dockerfile()`)
- **Story 2.5:** Resolver pipeline (`resolve_dockerfile()`)
- **Story 6.5:** Remote content analysis pattern (`compose_content` mirrors `dockerfile_content`)

### Source Tree

```
buncker/
  compose.py     # New module - Compose parser
  __main__.py    # Add --compose flag
  handler.py     # Add compose_content support
tests/
  buncker/
    test_compose.py  # New test file
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker/test_compose.py`
- **Framework:** pytest 8.x
- **Fixtures:** YAML strings in test file (no external fixture files needed)
- Use `tmp_path` for Dockerfile resolution in build context tests

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-09 | 0.1 | Initial draft from elicitation | Romain G. |
