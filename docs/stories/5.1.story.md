# Story 5.1: .deb Packaging

## Status

Ready for Review

## Story

**As a** release engineer,
**I want** both components packaged as .deb,
**so that** they can be installed on Debian/Ubuntu with proper dependency management.

## Acceptance Criteria

1. `make build-deb` produces `buncker` and `buncker-fetch` .deb files
2. `buncker.deb` installs: `/usr/bin/buncker`, `/usr/lib/buncker/`, `/etc/buncker/config.json`, `buncker.service`
3. `buncker-fetch.deb` installs: `/usr/bin/buncker-fetch`, `/usr/lib/buncker-fetch/`
4. Depends: `python3 (>= 3.11)`, `python3-cryptography`
5. `postinst` creates buncker user and directories
6. `dpkg -i` + `systemctl enable --now` = working daemon
7. `shared/` copied in each .deb
8. GitHub Actions builds .deb as artifacts

## Tasks / Subtasks

- [x] Task 1: Create buncker .deb packaging files (AC: 2, 4, 5, 7)
  - [x] `packaging/buncker/debian/control`: Package name, version, depends, description
  - [x] `packaging/buncker/debian/install`: File mapping to destination paths
  - [x] `packaging/buncker/debian/rules`: Build rules
  - [x] `packaging/buncker/debian/conffiles`: `/etc/buncker/config.json`
  - [x] `packaging/buncker/debian/postinst`: Create `buncker` user/group, create `/var/lib/buncker/`, set permissions
  - [x] `packaging/buncker/debian/buncker.service`: systemd unit (from Story 3.4)
  - [x] Include `shared/` in `/usr/lib/buncker/shared/`
- [x] Task 2: Create buncker-fetch .deb packaging files (AC: 3, 4, 7)
  - [x] `packaging/buncker-fetch/debian/control`: Package name, version, depends
  - [x] `packaging/buncker-fetch/debian/install`: File mapping
  - [x] `packaging/buncker-fetch/debian/rules`: Build rules
  - [x] Include `shared/` in `/usr/lib/buncker-fetch/shared/`
- [x] Task 3: Create entry point scripts (AC: 2, 3)
  - [x] `/usr/bin/buncker`: Shell wrapper that runs `python3 -m buncker`
  - [x] `/usr/bin/buncker-fetch`: Shell wrapper that runs `python3 -m buncker_fetch`
  - [x] Set `PYTHONPATH` to include the lib directory
- [x] Task 4: Add `build-deb` Makefile target (AC: 1)
  - [x] Build both .deb packages
  - [x] Use `dpkg-deb --build` with staging directory
  - [x] Output to `dist/` directory
- [x] Task 5: Add CI workflow for .deb building (AC: 8)
  - [x] Add job to GitHub Actions: build .deb on Ubuntu
  - [x] Upload .deb files as artifacts
  - [x] Run on push to main and tags
- [x] Task 6: Write tests (AC: 6)
  - [x] Test .deb can be built without errors
  - [x] Test package metadata is correct (dpkg-deb --info)
  - [x] Test file list is correct (dpkg-deb --contents)

## Dev Notes

### Package Structure - buncker.deb

```
/usr/bin/buncker                    # Entry point wrapper
/usr/lib/buncker/
  buncker/                          # Package source
    __init__.py, __main__.py, server.py, handler.py, ...
  shared/                           # Shared modules (copy)
    __init__.py, crypto.py, oci.py, wordlist.py, ...
/etc/buncker/config.json            # Default config (conffiles)
/lib/systemd/system/buncker.service # systemd unit
```

### Package Structure - buncker-fetch.deb

```
/usr/bin/buncker-fetch              # Entry point wrapper
/usr/lib/buncker-fetch/
  buncker_fetch/                    # Package source
    __init__.py, __main__.py, fetcher.py, ...
  shared/                           # Shared modules (copy)
    __init__.py, crypto.py, oci.py, wordlist.py, ...
```

### Dependencies (debian/control)

```
Depends: python3 (>= 3.11), python3-cryptography (>= 41.0)
```

No pip, no venv. Everything installed via apt.

[Source: `docs/architecture/3-tech-stack.md`, `docs/architecture/11-infrastructure-and-deployment.md`]

### Deployment Strategy

Manual .deb installation (air-gap requirement). No apt repository.

CI builds .deb as artifacts on GitHub Actions.

[Source: `docs/architecture/11-infrastructure-and-deployment.md`]

### Rollback

`dpkg -i buncker_<previous_version>.deb`. Store persists across upgrades. Config protected via `conffiles`.

[Source: `docs/architecture/11-infrastructure-and-deployment.md#Rollback`]

### Dependencies on Previous Stories

- **All previous stories:** Source code must be complete
- **Story 3.4:** systemd unit file
- **Story 1.1:** Makefile for build target

### Source Tree

```
packaging/
  buncker/debian/
    control, rules, install, buncker.service, postinst, conffiles
  buncker-fetch/debian/
    control, rules, install
Makefile               # build-deb target
.github/workflows/ci.yml  # .deb build job
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test:** Verify .deb builds and contains correct files
- **Use `dpkg-deb --info` and `dpkg-deb --contents`** to validate
- **CI testing:** Build job should succeed on Ubuntu runner
- **Coverage:** N/A (packaging, not code)

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-04 | 0.1 | Initial draft | Romain G. |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

None - no blocking issues encountered.

### Completion Notes List

- All packaging files created for both buncker and buncker-fetch .deb packages
- Entry point scripts use `python3 -m` with PYTHONPATH for clean module resolution
- Makefile `build-deb` target uses `dpkg-deb --build` with staging directories
- CI workflow adds `build-deb` job with artifact upload on push to main and tags
- Tests require `dpkg-deb` (Linux only) - will run in CI, skipped locally on non-Linux
- Default config.json included as conffile for buncker package
- systemd service reused from Story 3.4

### File List

- `packaging/buncker/debian/control` (new)
- `packaging/buncker/debian/install` (new)
- `packaging/buncker/debian/rules` (new)
- `packaging/buncker/debian/conffiles` (new)
- `packaging/buncker/debian/postinst` (new)
- `packaging/buncker/debian/buncker.service` (existing - unchanged)
- `packaging/buncker/etc/buncker/config.json` (new)
- `packaging/buncker/usr/bin/buncker` (new)
- `packaging/buncker-fetch/debian/control` (new)
- `packaging/buncker-fetch/debian/install` (new)
- `packaging/buncker-fetch/debian/rules` (new)
- `packaging/buncker-fetch/usr/bin/buncker-fetch` (new)
- `Makefile` (modified - added build-deb, clean targets)
- `.github/workflows/ci.yml` (modified - added build-deb job)
- `tests/test_packaging.py` (new)

## QA Results

### Review Date: 2026-03-04

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Solid packaging implementation. File structure, postinst, entry points, and Makefile are well-designed. The `define build_pkg` macro in the Makefile is clean and avoids duplication. Entry point wrappers correctly set PYTHONPATH. postinst is idempotent with proper user/group creation and permission hardening.

### Refactoring Performed

None - packaging files are not candidates for automated refactoring.

### Compliance Check

- Coding Standards: OK
- Project Structure: OK - follows `docs/architecture/10-source-tree.md`
- Testing Strategy: OK - `test_packaging.py` validates all structural requirements
- All ACs Met: OK (AC 1-8 covered)

### Improvements Checklist

- [ ] **VER-001** (high): Version in `packaging/buncker/debian/control` and `packaging/buncker-fetch/debian/control` is `0.5.0` but CHANGELOG/README declare `1.0.0`. The Makefile patches version at build time from `buncker/__init__.py`, but `__init__.py` likely still says `0.5.0`. Align all version sources to `1.0.0` before release.
- [ ] **PKG-001** (medium): `packaging/*/debian/rules` should add `override_dh_auto_test:` (empty no-op) to prevent debhelper from running tests during `dpkg-buildpackage`. Not critical since the Makefile uses `dpkg-deb --build` directly, but makes the rules files more robust.
- [ ] **CI-001** (medium): `ci.yml` `build-deb` job uploads artifacts via `upload-artifact` but does not create a GitHub Release. CONTRIBUTING.md claims tags trigger release uploads - this is not implemented. Add a `release` job triggered by `v*` tags that runs `gh release create`.
- [ ] **CI-002** (low): Pin `ruff` version in CI (`pip install ruff==0.x.y`) to avoid surprise breakage.
- [ ] **CI-003** (low): Add `pip cache` action for faster CI runs.

### Security Review

- postinst creates system user with `--no-create-home --shell /usr/sbin/nologin` - correct hardening
- Config file permissions set to 0640 owned by root:buncker - good
- Data directories owned by buncker:buncker with 0750 - appropriate
- No secrets in packaging files

### Performance Considerations

No performance concerns - packaging is a build-time operation.

### Files Modified During Review

None.

### Gate Status

Gate: CONCERNS -> docs/qa/gates/5.1-deb-packaging.yml

### Recommended Status

CONCERNS - Version inconsistency (VER-001) should be resolved before tagging v1.0.0. CI release job (CI-001) needed for the documented workflow.
