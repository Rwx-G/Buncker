# Story 7.3: Log Rotation

## Status

Draft

## Story

**As an** operator,
**I want** automatic log rotation for Buncker logs,
**so that** disk space is managed without manual intervention.

## Acceptance Criteria

1. `packaging/buncker/debian/logrotate` provides a `logrotate.d/buncker` config file
2. Config rotates `/var/log/buncker/*.log` daily, keeps 30 days, compresses old logs with gzip
3. Rotation uses `copytruncate` (daemon keeps file handle open, no signal-based rotation needed)
4. The logrotate config is installed to `/etc/logrotate.d/buncker` by both .deb and .rpm packages
5. .deb `conffiles` updated to include the logrotate config (preserved on upgrade)
6. Tests: verify logrotate config syntax is valid (`logrotate -d`), config file present in package

## Tasks / Subtasks

- [ ] Task 1: Create logrotate config file (AC: 1, 2, 3)
  - [ ] Create `packaging/buncker/logrotate` with rotation policy
  - [ ] Configure: daily, rotate 30, compress, delaycompress, missingok, notifempty, copytruncate
  - [ ] Target: `/var/log/buncker/*.log`
- [ ] Task 2: .deb packaging integration (AC: 4, 5)
  - [ ] Add logrotate config to `packaging/buncker/debian/install` file mapping
  - [ ] Add `/etc/logrotate.d/buncker` to `packaging/buncker/debian/conffiles`
- [ ] Task 3: RPM packaging integration (AC: 4)
  - [ ] Add logrotate config to `%files` section in `buncker.spec` (coordinated with Story 7.2)
  - [ ] Mark as `%config(noreplace)`
- [ ] Task 4: Write tests (AC: 6)
  - [ ] Verify logrotate config syntax with `logrotate -d` in CI
  - [ ] Verify config file is included in .deb package file list
  - [ ] Verify config file is included in .rpm package file list

## Dev Notes

### Logrotate Config

```
/var/log/buncker/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
```

`copytruncate` is the right choice because:
- The buncker daemon uses Python's `logging` module with a file handler
- The file handler keeps the file open
- `copytruncate` copies the file then truncates the original - no reopen needed
- No need for a `postrotate` script to send SIGHUP

### Dependencies on Previous Stories

- **Story 5.1:** .deb packaging (install file, conffiles)
- **Story 7.2:** RPM packaging (spec file %files section)

### Source Tree

```
packaging/
  buncker/
    logrotate           # New - logrotate config
    debian/
      install           # Updated - add logrotate mapping
      conffiles         # Updated - add logrotate path
```

### Testing

- **CI verification:** `logrotate -d /path/to/config` returns 0
- **Package verification:** `dpkg -c buncker.deb | grep logrotate` and `rpm -qpl buncker.rpm | grep logrotate`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-09 | 0.1 | Initial draft from elicitation | Romain G. |
