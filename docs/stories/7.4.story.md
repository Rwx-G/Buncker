# Story 7.4: OCI Auth Restriction

## Status

Draft

## Story

**As a** security-conscious operator,
**I want** to require authentication on OCI pull endpoints,
**so that** only authorized Docker clients can pull images in high-security environments.

## Acceptance Criteria

1. `--restrict-oci` flag on `buncker serve` enables auth on `/v2/*` endpoints
2. Config option `oci.restrict: true` persisted in `config.json` (default: `false`)
3. When enabled, `/v2/*` endpoints require a valid read-only or admin Bearer token
4. `GET /v2/` returns 401 with `WWW-Authenticate: Bearer realm="buncker"` when no valid token is present (standard OCI auth challenge)
5. Docker clients authenticate via `hosts.toml` configuration with the read-only token
6. When disabled (default), `/v2/*` behavior is unchanged (unauthenticated, backward compatible)
7. README updated with `hosts.toml` configuration example for restricted mode
8. Tests: pull with valid token succeeds, pull without token returns 401 with proper challenge, default mode unchanged

## Tasks / Subtasks

- [ ] Task 1: Add config option and CLI flag (AC: 1, 2)
  - [ ] Add `oci.restrict` field to config schema (default: `false`)
  - [ ] Add `--restrict-oci` flag to `buncker serve` argparse
  - [ ] Flag sets `oci.restrict: true` in running config
- [ ] Task 2: Extend auth middleware (AC: 3, 4, 6)
  - [ ] Modify `_check_auth()` in `handler.py` to check `oci.restrict` for `/v2/*` routes
  - [ ] When restricted: require read-only or admin token (same validation as `/admin/*` read-only)
  - [ ] Return 401 with `WWW-Authenticate: Bearer realm="buncker"` header on auth failure
  - [ ] When not restricted: existing behavior (no auth check on `/v2/*`)
- [ ] Task 3: Docker client documentation (AC: 5, 7)
  - [ ] Add `hosts.toml` example to README for restricted mode configuration
  - [ ] Document: `/etc/docker/certs.d/buncker:5000/hosts.toml` with `[host."https://buncker:5000"]` and `authorization` header
- [ ] Task 4: Write tests (AC: 8)
  - [ ] Test `/v2/` returns 401 with `WWW-Authenticate` header when restricted and no token
  - [ ] Test `/v2/{name}/manifests/{ref}` with valid read-only token returns manifest
  - [ ] Test `/v2/{name}/blobs/{digest}` with valid admin token returns blob
  - [ ] Test default mode (restrict=false): no auth required on `/v2/*`
  - [ ] Test restricted mode requires `api.enabled: true` (refuse if auth not configured)

## Dev Notes

### Auth Flow for Restricted OCI

```
Docker client                    buncker (--restrict-oci)
    |                                |
    |-- GET /v2/ ------------------>|
    |<-- 401 + WWW-Authenticate ----|
    |                                |
    |-- GET /v2/ + Bearer token --->|
    |<-- 200 OK --------------------|
    |                                |
    |-- GET /v2/name/manifests/tag ->|
    |   + Bearer token               |
    |<-- 200 + manifest ------------|
```

### WWW-Authenticate Header

```
WWW-Authenticate: Bearer realm="buncker",service="buncker",scope="repository:*:pull"
```

This follows the OCI Distribution Spec auth challenge format so Docker clients can be configured with static credentials.

### hosts.toml Example

```toml
# /etc/docker/certs.d/buncker:5000/hosts.toml
server = "https://buncker:5000"

[host."https://buncker:5000"]
  capabilities = ["pull"]
  ca = "/path/to/ca.pem"
  [host."https://buncker:5000".header]
    Authorization = ["Bearer <readonly-token>"]
```

### Guard: restrict requires auth

`--restrict-oci` without `api.enabled: true` is an error - refuse to start. Same pattern as "TLS required when auth enabled".

### Dependencies on Previous Stories

- **Story 6.1:** Token generation (tokens must exist)
- **Story 6.2:** Auth middleware (extend existing `_check_auth()`)

### Source Tree

```
buncker/
  handler.py    # Extend _check_auth() for /v2/* routes
  config.py     # Add oci.restrict config field
  __main__.py   # Add --restrict-oci flag
tests/
  buncker/
    test_handler.py  # Add restricted OCI tests
```

### Testing

- **Test location:** `tests/buncker/test_handler.py` (extended)
- **Framework:** pytest 8.x
- Use existing server integration test patterns from `test_server_integration.py`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-09 | 0.1 | Initial draft from elicitation | Romain G. |
