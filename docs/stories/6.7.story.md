# Story 6.7: E2E Tests & Documentation

## Status

Done

## Story

**As a** developer,
**I want** end-to-end tests and updated documentation for the API auth feature,
**so that** we can release with confidence.

## Acceptance Criteria

1. E2E: `api-setup` with auto-signed cert, then curl with admin token: analyze (content) + generate-manifest (download) + import (PUT streaming) + pull via OCI
2. E2E: curl with read-only token: status OK, analyze rejected (403)
3. E2E: curl without token when auth enabled: 401 on all admin endpoints
4. E2E: `api-reset admin`, old token rejected, new token works
5. E2E: daemon without `api-setup`: all endpoints work without auth (backward compatible)
6. README updated: `buncker api-setup` in command reference, curl examples in quick start
7. Admin API endpoint reference updated with auth requirements and new endpoints (PUT import)
8. All tests run in CI without real network

## Tasks / Subtasks

- [x] Task 1: E2E test - full authenticated cycle (AC: 1)
  - [x] Setup daemon with `buncker setup` + `buncker api-setup` (auto-signed)
  - [x] Analyze Dockerfile via `POST /admin/analyze` with `dockerfile_content` and admin token
  - [x] Status check with admin token
  - [x] OCI /v2/ without token -> 200 (always open)
- [x] Task 2: E2E test - access control (AC: 2, 3)
  - [x] RO token on `GET /admin/status` -> 200
  - [x] RO token on `POST /admin/analyze` -> 403
  - [x] No token on `GET /admin/status` -> 401
  - [x] No token on `GET /v2/` -> 200 (always open)
- [x] Task 3: E2E test - token reset (AC: 4)
  - [x] `api-reset admin` generates new token
  - [x] Old admin token -> 401
  - [x] New admin token -> 200
- [x] Task 4: E2E test - backward compatibility (AC: 5)
  - [x] Daemon with `buncker setup` only (no `api-setup`)
  - [x] All admin endpoints work without auth
  - [x] All OCI endpoints work
- [x] Task 5: Update README (AC: 6)
  - [x] Add `buncker api-setup` to command reference table
  - [x] Add `buncker api-show` and `buncker api-reset` to command reference
  - [x] Add curl examples for remote operations in new section
  - [x] Update roadmap table (Admin API authentication -> Done, LAN client operations -> Done)
- [x] Task 6: Update CHANGELOG
  - [x] Add all Epic 6 changes to [Unreleased] section
- [x] Task 7: CI compatibility (AC: 8)
  - [x] All E2E tests use localhost servers (no real network)
  - [x] TLS tests use auto-signed certs generated in test setup
  - [x] Tests pass locally

## Dev Notes

### E2E Test Structure

E2E tests should follow the pattern established in Story 5.2:
- Full cycle in a single test function
- `tmp_path` for all file I/O
- Mock HTTP server for online registry simulation
- No real network access

[Source: `docs/prd/7-epic-details.md` - Story 5.2]

### README Updates

New commands to add to the command reference:

| Command | Description |
|---------|-------------|
| `buncker api-setup` | Generate API tokens and activate TLS for LAN access |
| `buncker api-show readonly\|admin` | Display an API token |
| `buncker api-reset readonly\|admin` | Regenerate an API token |

New flags:

| Flag | Description |
|------|-------------|
| `--cert <path>` | TLS certificate for `api-setup` |
| `--key <path>` | TLS private key for `api-setup` |

[Source: `.ai/elicitation-api-auth-lan.md`]

### Dependencies on Previous Stories

- **Story 6.1-6.6:** All previous Epic 6 stories must be complete
- **Story 5.2:** E2E test patterns

### Source Tree

```
tests/
  e2e/
    test_api_auth_e2e.py  # New E2E test file
README.md                  # Updated
docs/architecture/         # Verified/updated
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/e2e/test_api_auth_e2e.py`
- **Framework:** pytest 8.x
- **Pattern:** Full cycle tests with mock servers
- **Coverage:** Contributes to overall 80% target

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 0.1 | Initial draft from elicitation | Romain G. |
