# Story 6.2: Auth Middleware

## Status

Done

## Story

**As a** daemon,
**I want** to validate Bearer tokens on admin endpoints,
**so that** only authorized clients can access registry operations.

## Acceptance Criteria

1. When `api.enabled: true`, all `/admin/*` requests must include `Authorization: Bearer <token>`
2. Read-only token grants access to: `GET /admin/status`, `GET /admin/logs`, `GET /admin/gc/report`
3. Admin token grants access to all `/admin/*` endpoints
4. `/v2/*` OCI endpoints remain unauthenticated regardless of auth configuration
5. Missing or invalid token returns 401 Unauthorized with a JSON error body
6. Read-only token on an admin-only endpoint returns 403 Forbidden
7. Token comparison uses constant-time comparison (`hmac.compare_digest`)
8. When `api.enabled: false` (no `api-setup` run), all endpoints behave as before (no auth)
9. Tests: valid RO token on RO endpoint, valid admin token on admin endpoint, RO token on admin endpoint rejected, invalid token rejected, no auth when disabled

## Tasks / Subtasks

- [x] Task 1: Implement auth middleware in `auth.py` (AC: 1, 2, 3, 7)
  - [x] Create `authenticate_request(handler, required_level)` function
  - [x] Extract `Authorization: Bearer <token>` from request headers
  - [x] Compare token against loaded tokens using `hmac.compare_digest`
  - [x] Return auth level (`admin`, `readonly`) or raise appropriate error
- [x] Task 2: Define endpoint access matrix (AC: 2, 3)
  - [x] Map each `/admin/*` endpoint to its required access level
  - [x] Read-only: `GET /admin/status`, `GET /admin/logs`, `GET /admin/gc/report`
  - [x] Admin: `POST /admin/analyze`, `POST /admin/generate-manifest`, `POST /admin/import`, `PUT /admin/import`, `POST /admin/gc/execute`
- [x] Task 3: Integrate auth into handler (AC: 1, 4, 8)
  - [x] Call auth middleware at the start of each `/admin/*` handler method
  - [x] Skip auth entirely for `/v2/*` endpoints
  - [x] Skip auth when `api.enabled: false`
- [x] Task 4: Error responses (AC: 5, 6)
  - [x] 401 Unauthorized: `{"error": "Authentication required", "code": "AUTH_REQUIRED"}`
  - [x] 403 Forbidden: `{"error": "Insufficient permissions", "code": "FORBIDDEN"}`
- [x] Task 5: Write tests (AC: 9)
  - [x] Test valid RO token on RO endpoint -> 200
  - [x] Test valid admin token on admin endpoint -> 200
  - [x] Test valid admin token on RO endpoint -> 200
  - [x] Test valid RO token on admin endpoint -> 403
  - [x] Test invalid token -> 401
  - [x] Test missing token -> 401
  - [x] Test `/v2/*` without token -> 200 (always open)
  - [x] Test all endpoints with `api.enabled: false` -> no auth required

## Dev Notes

### Endpoint Access Matrix

| Endpoint | Required Level |
|----------|---------------|
| `GET /admin/status` | readonly |
| `GET /admin/logs` | readonly |
| `GET /admin/gc/report` | readonly |
| `POST /admin/analyze` | admin |
| `POST /admin/generate-manifest` | admin |
| `POST /admin/import` | admin |
| `PUT /admin/import` | admin |
| `POST /admin/gc/execute` | admin |

[Source: `docs/prd/7-epic-details.md`, `.ai/elicitation-api-auth-lan.md`]

### Security Notes

- Token comparison MUST use `hmac.compare_digest` to prevent timing attacks
- Error responses MUST NOT reveal whether the token was close to valid
- Tokens are NEVER logged (NFR7)

[Source: `docs/architecture/15-security.md`]

### Dependencies on Previous Stories

- **Story 6.1:** Token generation & storage (provides `load_api_tokens()`)
- **Story 3.2:** Admin API (handler routes to protect)
- **Story 3.1:** HTTP server (OCI routes to keep open)

### Source Tree

```
buncker/
  auth.py       # Auth middleware (extend from Story 6.1)
  handler.py    # Modified - add auth calls
tests/
  buncker/
    test_auth.py  # Extended with middleware tests
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker/test_auth.py`
- **Framework:** pytest 8.x
- **Integration pattern:** Server on `localhost:0`, requests with/without Authorization header
- **Coverage:** 80% minimum

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 0.1 | Initial draft from elicitation | Romain G. |
