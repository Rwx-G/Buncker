# Story 6.4: Token Management Commands

## Status

Draft

## Story

**As an** operator,
**I want** to view and regenerate API tokens after initial setup,
**so that** I can manage access without re-running full setup.

## Acceptance Criteria

1. `buncker api-show readonly` displays the current read-only token
2. `buncker api-show admin` displays the current admin token
3. `buncker api-reset readonly` generates a new read-only token, invalidates the previous one, and displays the new token
4. `buncker api-reset admin` generates a new admin token, invalidates the previous one, and displays the new token
5. All commands are local-only (CLI, not exposed via HTTP)
6. Token reset is logged as an audit event (`api_token_reset` with token type)
7. Tests: show displays correct token, reset changes token, old token rejected after reset

## Tasks / Subtasks

- [ ] Task 1: Add `api-show` subcommand (AC: 1, 2, 5)
  - [ ] Register `api-show` in `__main__.py` argparse with positional arg `readonly|admin`
  - [ ] Load tokens from `/etc/buncker/api-tokens.json`
  - [ ] Display the requested token
  - [ ] Error if `api-setup` hasn't been run (file doesn't exist)
- [ ] Task 2: Add `api-reset` subcommand (AC: 3, 4, 5)
  - [ ] Register `api-reset` in `__main__.py` argparse with positional arg `readonly|admin`
  - [ ] Generate new 256-bit token via `secrets.token_hex(32)`
  - [ ] Update the specific token in `api-tokens.json` (keep the other unchanged)
  - [ ] Display the new token
- [ ] Task 3: Audit logging for reset (AC: 6)
  - [ ] Log `api_token_reset` event with `token_type` field (`readonly` or `admin`)
  - [ ] Do NOT log the token value itself
- [ ] Task 4: Write tests (AC: 7)
  - [ ] Test `api-show readonly` returns correct token
  - [ ] Test `api-show admin` returns correct token
  - [ ] Test `api-show` without prior `api-setup` returns error
  - [ ] Test `api-reset readonly` changes the RO token, admin unchanged
  - [ ] Test `api-reset admin` changes the admin token, RO unchanged
  - [ ] Test old token rejected after reset (integration with auth middleware)
  - [ ] Test audit log entry produced on reset

## Dev Notes

### CLI Interface

```bash
buncker api-show readonly    # Display read-only token
buncker api-show admin       # Display admin token
buncker api-reset readonly   # Regenerate read-only token
buncker api-reset admin      # Regenerate admin token
```

These commands are CLI-only and not exposed via HTTP endpoints.

[Source: `.ai/elicitation-api-auth-lan.md`]

### Dependencies on Previous Stories

- **Story 6.1:** Token generation & storage (token file format, `load_api_tokens()`)
- **Story 6.2:** Auth middleware (to verify old token is rejected after reset)
- **Story 1.4:** Logging setup

### Source Tree

```
buncker/
  __main__.py   # Add api-show, api-reset subcommands
  auth.py       # Add reset_token() function
tests/
  buncker/
    test_auth.py  # Extended with show/reset tests
```

[Source: `docs/architecture/10-source-tree.md`]

### Testing

- **Test location:** `tests/buncker/test_auth.py`
- **Framework:** pytest 8.x
- **Coverage:** 80% minimum
- Use `tmp_path` for token file operations

[Source: `docs/architecture/14-test-strategy.md`]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 0.1 | Initial draft from elicitation | Romain G. |
